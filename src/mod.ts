/**
 * wmdb-ts - A TypeScript library for Deno
 *
 * @module
 */

/**
 * Example function to demonstrate the library structure
 * @param name - The name to greet
 * @returns A greeting message
 */
export function greet(name: string): string {
  return `Hello, ${name}!`;
}

/**
 * Example version constant
 */
export const VERSION = "0.1.0";
